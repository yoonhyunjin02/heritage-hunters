spring:
  datasource:
    url: jdbc:postgresql://heritage-hunters.cx268qu2uk1o.eu-north-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: hh_password
    driver-class-name: org.postgresql.Driver

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: update  # 개발 시 테이블 자동 수정
    show-sql: true

  # Google OAuth
  security:
    user:
      name: admin
      password: 1234
#    oauth2:
#      client:
#        registration:
#          google:
#            client-id: ${GOOGLE_CLIENT_ID}
#            client-secret: ${GOOGLE_CLIENT_SECRET}
#            scope: ${GOOGLE_SCOPE}
#            redirect-uri: ${GOOGLE_REDIRECT_URI}

# 로그
logging:
  level:
    org.hibernate.SQL: INFO
    org.hh.heritagehunters: DEBUG